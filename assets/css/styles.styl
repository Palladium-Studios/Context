global-reset()

@import 'chat'
@import 'login'

// Forms
input[type=text]
input[type=password]
button
	display flex
	font-size 18pt
	padding 5px

.app-container
	width 100%
	height 100%
	display flex
	flex-flow row
	margin 0
