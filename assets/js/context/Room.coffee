angular.module('Context').service 'Room', () ->

	Rooms = {}

	return {
		###
			Sends a request for a new room,
			stores a reference to the room,
			returns a reference to the room.
		###
		new : (users) ->

		get : (users) ->

	}